#include "main.h"

int mimi(char *s);
int is_palindrome(char *s);
int father(char *s, int local, int fat);

/**
 * is_palindrome -  returns 1 if a string is a palindrome and 0 if not
 * @s: String to measured
 * Return: Length of string
 */
int mimi(char *s)
{
	int local = 0;

	if (*(s * local))
	{

